services:
  gatus:
    # Fix version to control upgrades
    image: twinproduction/gatus:v5.36.0
    env_file:
      - gatus.env
    ports:
      - "${GATUS_EXT_PORT}:${GATUS_INT_PORT}"
    volumes:
      - "${GATUS_DIR}:${GATUS_DIR}"
